# 🚀 Деплой — Hugging Face Spaces

```bash
git push --force space main
```

Переменные окружения в Settings → Variables and Secrets:
- `JWT_SECRET` (128+ символов)
- `ENCRYPTION_KEY` (64 hex-символа)
- `NODE_ENV` = `production`
- `PORT` = `7860`
